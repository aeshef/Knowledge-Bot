ЗАДАЧА: Суммаризуй текст транскрибации в связный, читабельный конспект без воды.

ВХОД:
- asr_text — полный текст транскрипции (может содержать повторы, «эээ», незавершённые фразы);
- type — конечный тип заметки (для стилистики и фокуса) — опционально.

ТРЕБОВАНИЯ:
- Кратко, но содержательно; структурируй абзацами и/или списками, если уместно.
- Удали паразитные слова и повторы; сохрани факты, формулируй ясно.
- Без заголовков, без фронтматтера, только чистый Markdown‑текст.
- Язык исходный (не переводить), не добавляй эмодзи.

ВЫХОД (строгий JSON): {"asr_summary": "..."}

